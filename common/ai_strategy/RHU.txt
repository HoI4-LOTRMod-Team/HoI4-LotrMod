RHU_default_strategy = {
    enable = {
        always = yes
    }
    allowed = {
        original_tag = RHU
    }

    abort_when_not_enabled = yes
	abort = {
		OR = {
			has_war_with = DOR
			has_war_with = LGT
			has_civil_war = yes
			NOT = { 104 = { is_fully_controlled_by = RHU } }
			NOT = { 130 = { is_fully_controlled_by = RHU } }
			NOT = { 129 = { is_fully_controlled_by = RHU } }
		}
    }

    ai_strategy = {
        type = prepare_for_war
        id = ERE
        value = 50
    }
	###dont fuck around in the sand strategy
    ai_strategy = {
        type = area_priority
        id = rhun_deserts
        value = -50
    }
	###dont interfere with mordor-harad-gondor war
    ai_strategy = {
        type = area_priority
        id = mordor
        value = -50
    }

    ai_strategy = {
        type = area_priority
        id = harad
        value = -50
    }
	
    ai_strategy = {
        type = area_priority
        id = gondor
        value = -50
    }
}
	
