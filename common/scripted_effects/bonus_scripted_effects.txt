remove_new_trait = {#Getting rid of the 'new' trait when switching GUN's race
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = chief_advisor
		}
		remove_trait = {
			slot = chief_advisor
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = spymaster
		}
		remove_trait = {
			slot = spymaster
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = treasurer
		}
		remove_trait = {
			slot = treasurer
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = political_advisor
		}
		remove_trait = {
			slot = political_advisor
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = theorist
		}
		remove_trait = {
			slot = theorist
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = army_chief
		}
		remove_trait = {
			slot = army_chief
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = cavalry_chief
		}
		remove_trait = {
			slot = cavalry_chief
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = navy_chief
		}
		remove_trait = {
			slot = navy_chief
			trait = new
		}
	}
	every_character = {
		limit = {
			has_trait = new
			is_character_slot = high_command
		}
		remove_trait = {
			slot = high_command
			trait = new
		}
	}
}