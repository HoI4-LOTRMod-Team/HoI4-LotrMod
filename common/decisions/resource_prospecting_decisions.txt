


prospecting_for_resources = {


	# prospect for new fertile soil and fishing grounds
	prospect_for_oil = {
		icon = decision_farm_food

		fire_only_once = yes
		cost = 50

		days_remove = 90

		allowed = {
			always = yes
		}

		target_root_trigger = {
			has_country_flag = resource_prospecting_unlocked
		}

		available = {
			num_of_available_civilian_factories > 2
			OR = {
				has_tech = improved_oil_processing
				has_tech = rubber_processing
			}
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = oil_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 3
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = oil
					amount = var:oil_deposit
				}
				clear_variable = oil_deposit
			}
		}
	}


	# prospect for new hunting grounds
	prospect_for_rubber = {
		icon = decision_leather_tannery

		fire_only_once = yes
		cost = 50

		days_remove = 90

		allowed = {
			always = yes
		}

		target_root_trigger = {
			has_country_flag = resource_prospecting_unlocked
		}

		available = {
			num_of_available_civilian_factories > 2
			OR = {
				has_tech = advanced_rubber_processing
				has_tech = advanced_oil_processing
			}
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = rubber_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 3
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = rubber
					amount = var:rubber_deposit
				}
				clear_variable = rubber_deposit
			}
		}
	}


	# find new mithril veins in the mountains
	prospect_for_mithril = {
		icon = decision_mithril

		fire_only_once = yes
		cost = 50

		days_remove = 180

		allowed = {
			always = yes
		}

		target_root_trigger = {
			has_country_flag = mithril_prospecting_unlocked
		}

		available = {
			num_of_available_civilian_factories > 3
			has_tech = excavation4
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = aluminium_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 4
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = aluminium
					amount = var:aluminium_deposit
				}
				clear_variable = aluminium_deposit
			}
		}
	}

	# prospect for new silk and hemp growth areas
	prospect_for_tungsten = {
		icon = decision_string

		fire_only_once = yes
		cost = 50

		days_remove = 120

		allowed = {
			always = yes
		}

		target_root_trigger = {
			has_country_flag = resource_prospecting_unlocked
		}

		available = {
			num_of_available_civilian_factories > 2
			has_tech = fertilizer
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = tungsten_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 3
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = tungsten
					amount = var:tungsten_deposit
				}
				clear_variable = tungsten_deposit
			}
		}
	}

	# prospect for new iron deposits
	prospect_for_steel = {
		icon = decision_iron_steel

		fire_only_once = yes
		cost = 50

		days_remove = 90

		allowed = {
			always = yes
		}

		target_root_trigger = {
			has_country_flag = resource_prospecting_unlocked
		}

		available = {
			num_of_available_civilian_factories > 2
			has_tech = excavation3
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = steel_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 3
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = steel
					amount = var:steel_deposit
				}
				clear_variable = steel_deposit
			}
		}
	}

	# prospect for new lumber sources
	prospect_for_chromium = {
		icon = decision_lumber_wood

		fire_only_once = yes
		cost = 50

		days_remove = 70

		allowed = {
			always = yes
		}

		target_root_trigger = {
			has_country_flag = resource_prospecting_unlocked
		}

		available = {
			num_of_available_civilian_factories > 3
			has_tech = construction3
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = chromium_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 4
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = chromium
					amount = var:chromium_deposit
				}
				clear_variable = chromium_deposit
			}
		}
	}

	# prospect for new coal mines
	prospect_for_coal = {
		icon = decision_coal_charcoal

		fire_only_once = yes
		cost = 50

		days_remove = 90

		allowed = {
			always = yes
		}

		target_root_trigger = {
			has_country_flag = resource_prospecting_unlocked
		}

		available = {
			num_of_available_civilian_factories > 2
			has_tech = excavation2
			has_tech = advanced_machine_tools
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = coal_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 3
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = coal
					amount = var:coal_deposit
				}
				clear_variable = coal_deposit
			}
		}
	}


}