


prospect_for_resources = {


	prospect_for_oil = {
		icon = generic_research

		fire_only_once = yes
		cost = 50

		days_remove = 70

		allowed = {
			always = yes
		}

		target_root_trigger = {
			num_of_available_civilian_factories > 2
			# TODO: has unlocked resource prospecting
			# TODO: has necessary technology
		}

		available = {
			always = yes # if yes, then this decision can be selected, otherwise it can't
		}

		state_target = yes
		
		target_array = controlled_states
		
		target_trigger = {
			# NOTE: Performance could be improved here by adding the respective states to an array or similar.
			FROM = {
				is_fully_controlled_by = ROOT
				has_variable = oil_deposit
			}
		}
		
		modifier = {
			civilian_factory_use = 3
		}

		remove_effect = {
			FROM = {
				add_resource = {
					type = oil
					amount = var:oil_deposit
				}
				clear_variable = oil_deposit
			}
		}
	}


}