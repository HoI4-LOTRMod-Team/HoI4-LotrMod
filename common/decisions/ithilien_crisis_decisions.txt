####################
##ITHILIEN CRISIS###
####################

ithilien_crisis_decisions_category = {

	ithilien_crisis_increase_orc_influence = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	FROM = {
	        	add_dynamic_modifier = {
				    modifier = orc_raiding_0
				    scope = FROM
				    days = 14
				}
			}
        }
	}

	###
	### SUBTERFUGE
	###

	ithc_subterfuge_sabotage_infrastructure = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# disable strat-redeployment, lower movement speed
        	# On war start, lower movement speed even more
        }
	}

	ithc_subterfuge_locate_defenses = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# On war start, decrease enemy defense and entrenchment, increase attack
        }
	}

	ithc_subterfuge_gather_intelligence = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# give intel boost
        }
	}

	ithc_subterfuge_hide_weapon_caches = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# On war start +supplies and -encirclement penalty for MOR
        }
	}

	ithc_subterfuge_plant_sleeper_cells = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# on war start: spawn divisions
        }
	}


	###
	### PILLAGE
	###

	ithc_pillage_ambush_the_garrison = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# deal damage to troops stationed in the state
        }
	}

	ithc_pillage_massacre_the_population = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# add resistance
        	# lower population
        }
	}

	ithc_pillage_lay_waste = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# add resistance
        	# add damage to buildings
        	# lower repair speed
        }
	}

	ithc_pillage_plunder = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# take factories from state
        	# (This should require min. X amount of resistance)
        }
	}

	ithc_pillage_burn_and_sear_the_fields = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# remove food, add damage, other stuff
        }
	}

}
