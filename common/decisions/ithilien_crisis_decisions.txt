####################
##ITHILIEN CRISIS###
####################

ithilien_crisis_decisions_category = {

	###
	### SUBTERFUGE
	###

	ithc_counter_subterfuge_operation_testeroo = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	#always = yes
	    	FROM = { has_state_flag = ithc_subterfuge_operation }
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
    		#FROM = { has_state_flag = ithc_subterfuge_operation }
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	
        }

        cancel_effect = {

        }

        complete_effect = {
        	FROM = { clr_state_flag = ithc_subterfuge_operation }
    	}
	}

	ithc_new_subterfuge_operation_testeroo = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	#always = yes
	    	FROM = { has_state_flag = ithc_subterfuge_operation }
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
    		#FROM = { has_state_flag = ithc_subterfuge_operation }
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	
        }

        cancel_effect = {

        }

        complete_effect = {
        	ithc_subterfuge_enable_new_decision = yes
    	}
	}



	ithc_subterfuge_sabotage_infrastructure = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 10

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	has_state_flag = ithc_subterfuge_sabotage_infrastructure_enabled
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		#always = no
    		FROM = { NOT = { has_state_flag = ithc_subterfuge_operation } }
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	FROM = { set_state_flag = ithc_infrastructure_sabotaged }
        	ithc_subterfuge_enable_new_decision = yes
        }

        cancel_effect = {

        }

        complete_effect = {
        	FROM = { set_state_flag = ithc_subterfuge_operation }
    	}
	}

	ithc_subterfuge_locate_defenses = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	has_state_flag = ithc_subterfuge_locate_defenses_enabled
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	FROM = { set_state_flag = ithc_defenses_located }
        	ithc_subterfuge_enable_new_decision = yes
        }
	}

	ithc_subterfuge_gather_intelligence = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	has_state_flag = ithc_subterfuge_gather_intelligence_enabled
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# give intel boost
        	ithc_subterfuge_enable_new_decision = yes
        }
	}

	ithc_subterfuge_hide_weapon_caches = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	has_state_flag = ithc_subterfuge_hide_weapon_caches_enabled
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# On war start +supplies and -encirclement penalty for MOR
        	FROM = { set_state_flag = ithc_hidden_weapon_caches }
        	ithc_subterfuge_enable_new_decision = yes
        }
	}

	ithc_subterfuge_plant_sleeper_cells = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	has_state_flag = ithc_subterfuge_plant_sleeper_cells_enabled
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# on war start: spawn divisions
        	FROM = { set_state_flag = ithc_sleeper_cells_planted }
        	ithc_subterfuge_enable_new_decision = yes
        }
	}


	###
	### PILLAGE
	###

	ithc_pillage_ambush_the_garrison = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# deal damage to troops stationed in the state
        }
	}

	ithc_pillage_massacre_the_population = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# add resistance
        	# lower population
        }
	}

	ithc_pillage_lay_waste = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# add resistance
        	# add damage to buildings
        	# lower repair speed
        }
	}

	ithc_pillage_plunder = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# take factories from state
        	# (This should require min. X amount of resistance)
        }
	}

	ithc_pillage_burn_and_sear_the_fields = {
		icon = mission_icon

		cost = 75
		state_target = yes
		targets = { 22 175 173 135 }
		is_good = yes
	    selectable_mission = yes
	    days_remove = 1

	    target_root_trigger = {
	        # TODO: only show when correct focus taken or whatever
	        tag = GON
	    }

	    target_trigger = {
	    	# TODO: only show this for states MOR doesnt own and stuff
	        tag = GON
	    }

	    available = {
	    	# TODO?
	    	always = yes
	    }

	    cancel_trigger = {
	    	# TODO: war breaks out etc.
    		always = no
	    }

	    ai_will_do = {
		    base = 10
		}

        remove_effect = {
        	# remove food, add damage, other stuff
        }
	}

}
