ring_decisions = {
	icon = ring_icon

	# scripted_gui = ring_decisions_scripted_gui # Disabled because we moved it elsewhere

	# visible_when_empty = yes

	priority = 90

	visible = {
		has_global_flag = fs_started
		NOT = { has_global_flag = fs_ring_seized }
		NOT = { has_global_flag = fs_ring_destroyed }
		# LTH needs to unlock this manually
		OR = {
			NOT = { tag = LTH }
			has_completed_focus = lth_theonering
		}
	}
}

# Sauron's decisions to corrupt ring owner (TODO (for the future): This should probably be - at least in part - part of the dedicated ring mechanic menu)
ring_corruption_decisions = {
	icon = ring_icon

	scripted_gui = ring_corruption_decisions_scripted_gui

	visible_when_empty = yes
	priority = 90

	allowed = {
		tag = MOR
	}

	visible = {
		OR = {
			has_global_flag = fs_ring_seized
			has_global_flag = fs_ring_destroyed
		}
		NOT = { ring_has_the_ring = yes }
	}
}


ring_corruption_decisions_generic = {
	icon = ring_icon

	scripted_gui = ring_corruption_decisions_generic_scripted_gui

	visible_when_empty = yes
	priority = 90

	allowed = {
		NOT = { tag = MOR }
	}

	visible = {
		has_global_flag = fs_ring_seized
		ring_has_the_ring = yes
	}
}