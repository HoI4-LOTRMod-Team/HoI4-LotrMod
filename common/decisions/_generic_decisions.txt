political_actions = {
	improved_worker_conditions = {

		icon = generic_2

		available = {
			has_stability < 1.0
		}

		visible = {
			NOT = {
				original_tag = MOR
			}
		}

		fire_only_once = no

		modifier = {
			stability_weekly = 0.005
			consumer_goods_factor = 0.05
			industrial_capacity_factory = -0.1
		}

		days_remove = 180
		days_re_enable = 540

		cost = 100

		complete_effect = {

		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_stability < 0.6
				has_political_power > 175 #Spend on other things first
			}
		}
	}

	food_shortages_upcoming = {
		icon = GFX_decision_generic_farming

		fire_only_once = no
		days_re_enable = 100
		days_mission_timeout = 35
		selectable_mission = no
		is_good = no

		allowed = {
			always = yes
		}

		visible = {
			date > "3018.2.1"
		}

		activation = {
			has_fuel < 10000
		}

		available = {
			has_fuel > 10000
		}

		timeout_effect = {
			# when the timer runs out uninterrupted
			add_ideas = food_shortages
			activate_mission = food_scarcity_upcoming
		}
	}

	food_scarcity_upcoming = {
		icon = GFX_decision_evil_web

		fire_only_once = no
		days_re_enable = 100
		days_mission_timeout = 70
		selectable_mission = no
		is_good = no

		allowed = {
			always = no
		}

		activation = {
			always = no
		}

		available = {
			has_fuel > 10000
		}

		timeout_effect = {
			# when the timer runs out uninterrupted
			swap_ideas = {
				remove_idea = food_shortages
				add_idea = food_scarcity
			}
			if = {
				limit = { NOT = { has_government = revolutionary } }
				add_popularity = {
					ideology = revolutionary
					popularity = 0.05
				}
			}
			activate_mission = famine_upcoming
		}
	}

	famine_upcoming = {
		icon = GFX_decision_evil_web

		fire_only_once = no
		days_re_enable = 100
		days_mission_timeout = 70
		selectable_mission = no
		is_good = no

		allowed = {
			always = no
		}

		activation = {
			always = no
		}

		available = {
			has_fuel > 10000
		}

		timeout_effect = {
			# when the timer runs out uninterrupted
			if = {
				limit = { NOT = { has_government = revolutionary } }
				add_popularity = {
					ideology = revolutionary
					popularity = 0.05
				}
			}
			swap_ideas = {
				remove_idea = food_scarcity
				add_idea = famine
			}
			
		}
	}

	food_rationing = {
		icon = GFX_decision_generic_farming

		fire_only_once = no # allow decision to reappear
		days_re_enable = 140 # if above set to no, this is the cooldown for re-appear
		cost = 150

		days_remove = 70

		allowed = {
			always = yes
		}

		visible = {
			has_fuel < 35000
		}

		available = {
			always = yes
		}

		ai_will_do = {
			base = 0
			modifier = {
				factor = 0
				is_gaining_fuel = yes
			}
			modifier = {
				add = 1
				fuel_ratio < 0.4
				has_stability > 0.6
			}
			modifier = {
				add = 2
				fuel_ratio < 0.3
				has_stability > 0.5
			}
			modifier = {
				add = 3
				fuel_ratio < 0.15
				has_stability > 0.5
			}
		}

		modifier = {
			food_use_from_population_factor = -0.25
			stability_weekly = -0.005
		}
	}

	farm_subsidies = {
		icon = GFX_decision_generic_farming

		fire_only_once = no # allow decision to reappear
		days_re_enable = 140 # if above set to no, this is the cooldown for re-appear
		cost = 75

		days_remove = 70

		allowed = {
			always = yes
		}

		visible = {
			has_fuel < 65000
			num_of_available_civilian_factories > 2
		}

		available = {
			always = yes
		}

		ai_will_do = {
			base = 0			modifier = {
				add = 1
				fuel_ratio < 0.5
			}
			modifier = {
				add = 2
				fuel_ratio < 0.3
			}
			modifier = {
				add = 3
				fuel_ratio < 0.15
			}
			modifier = {
				factor = resource@oil
			}
			modifier = {
				factor = 0
				is_gaining_fuel = yes
			}
		}

		modifier = {
			civilian_factory_use = 3
			fuel_gain_factor_from_states = 0.5
		}
	}

	emergency_weapons_stockpile = {
		icon = GFX_decision_civilian_life_peasants

		fire_only_once = yes
		cost = 15

		allowed = {
			always = yes
		}

		visible = {
			has_country_flag = defensive_weapons_stockpile
		}

		ai_will_do = {
			base = 100
		}

		available = {
			OR = {
				has_defensive_war = yes
				surrender_progress > 0.5
			}
		}
		
		complete_effect = {
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 1000
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = 250
			}
		}
	}

	emergency_musters = {
		icon = GFX_decision_generic_army_1

		fire_only_once = yes
		cost = 15

		allowed = {
			always = yes
		}

		ai_will_do = {
			base = 100
		}

		visible = {
			has_country_flag = defensive_musters
		}

		available = {
			OR = {
				has_defensive_war = yes
				surrender_progress > 0.5
			}
		}
		
		complete_effect = {
			add_manpower = 1500
		}
	}

	delayed_war_declaration = {
		icon = GFX_resistance_attack_war

		fire_only_once = no
		days_mission_timeout = 70
		selectable_mission = no
		is_good = no

		allowed = {
			always = no
		}

		activation = {
			always = no
		}

		#war_with_on_timeout = var:delayed_war_declaration_target # This doesn't seem to work. I'm using a warning event instead

		complete_effect = {
			hidden_effect = {
				var:delayed_war_declaration_target = {

					country_event = lotr_generic.132 # Warning event

					add_ai_strategy = {
						type = prepare_for_war
						id = ROOT
						value = 200
					}
					
				}
			}
		}

		available = {
			OR = {
				is_ally_with = var:delayed_war_declaration_target
				has_non_aggression_pact_with = var:delayed_war_declaration_target
				NOT = { is_neighbor_of = var:delayed_war_declaration_target }
			}
		}

		timeout_effect = {
			# when the timer runs out uninterrupted
			declare_war_on = {
				target = var:delayed_war_declaration_target
				type = puppet_wargoal_focus
			}
		}
	}
}

economy_decisions = {
	restructure_supply_system = { #one time boost to get a few cheap supply hubs

		icon = GFX_decision_generic_construction

		visible = { 
			OR = {
				is_major = no
				AND = {
					is_faction_leader = yes
					num_of_factories < 51 #would be a minor if it wasn't for faction leader status
				}
			}
		}
		fire_only_once = yes

		days_remove = 120

		ai_will_do = {
			factor = 1
			modifier = {
				num_of_factories < 30
				factor = 0
			}
		}

		modifier = {
			production_speed_supply_node_factor = 3
		}

		complete_effect = {
			set_variable = { node_goal = num_of_supply_nodes }
			add_to_variable = { node_goal = 3 } #maximum 3 additional nodes
		}

		cancel_trigger = {
			
			 check_variable = { node_goal < num_of_supply_nodes } #if the number of supply nodes exceeds the old number by +3, immediately cancel
		}
	}
	
	seize_some_trains_woo = { #repeating train seize

		icon = generic_4	

		visible = { 
			num_of_factories < 60
		}
		
		available = {
			has_equipment = { train_equipment < 101 }
			has_tech = trade_caravan
		}
		
		days_remove = 180

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_stability > 0.74
			}
		}

		complete_effect = {
			add_equipment_to_stockpile = {
				type = train_equipment
				amount = 15
			}
			add_stability = -0.05
		}
	}	
	
	infrastructure_building_slot = {

		icon = generic_construction

        allowed = {
            always = yes
        }

        cost = 100
        fire_only_once = no

        days_re_enable = 30

		available = {
			any_owned_state = {
				NOT = { has_state_flag = infrastructure_building_slot_flag }
				is_fully_controlled_by = ROOT
				free_building_slots = {
					building = infrastructure
					size < 1
				}
			}
		}

		visible = {
			any_owned_state = {
				NOT = { has_state_flag = infrastructure_building_slot_flag }
				is_fully_controlled_by = ROOT
				free_building_slots = {
					building = infrastructure
					size < 1
				}
			}
		}

        complete_effect = {
			random_owned_state = {
				limit = {
					is_fully_controlled_by = ROOT
            		free_building_slots = {
						building = industrial_complex
						size > 1
						include_locked = yes
					}
					free_building_slots = {
						building = infrastructure
						size < 1
					}
					NOT = { has_state_flag = infrastructure_building_slot_flag }
				}
				add_extra_state_shared_building_slots = 1
				set_state_flag = infrastructure_building_slot_flag
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				factor = 10
				has_political_power > 175 #Spend on other things first
				any_owned_state = { free_building_slots = { building = industrial_complex size < 1 include_locked = no } }
			}
		}
    }

    war_building_slot = {

    	icon = generic_construction

        allowed = {
            always = yes
        }

        cost = 50
        fire_only_once = no

        available = {
			IF = { 
				limit = { 
					OR = { 
						has_dlc = "Man the Guns" 	
						has_dlc = "By Blood Alone" 
					}
				}
				has_war_with_non_exiled_country = yes   
			}
			ELSE = {
				has_war = yes
			}
			any_owned_state = {
				NOT = { has_state_flag = war_building_slot_flag }
				is_fully_controlled_by = ROOT
			}
		}

        visible = {
			has_war = yes
			any_owned_state = {
				NOT = { has_state_flag = war_building_slot_flag }
				is_fully_controlled_by = ROOT
			}
		}

        complete_effect = {
        	add_war_support = -0.025
			add_stability = -0.025
			if = {
				limit = {
					any_owned_state = {
						is_fully_controlled_by = ROOT
            			free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = yes
						}
						NOT = { has_state_flag = war_building_slot_flag }
						infrastructure > 4
					}
				}
				random_owned_state = {
					limit = {
						is_fully_controlled_by = ROOT
            			free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = yes
						}
						NOT = { has_state_flag = war_building_slot_flag }
						infrastructure > 4
					}
					add_extra_state_shared_building_slots = 1
					set_state_flag = war_building_slot_flag
				}
			}
			else_if = {
				limit = {
					any_owned_state = {
						is_fully_controlled_by = ROOT
            			free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = yes
						}
						NOT = { has_state_flag = war_building_slot_flag }
						infrastructure > 2
					}
				}
				random_owned_state = {
					limit = {
						is_fully_controlled_by = ROOT
            			free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = yes
						}
						NOT = { has_state_flag = war_building_slot_flag }
						infrastructure > 2
					}
					add_extra_state_shared_building_slots = 1
					set_state_flag = war_building_slot_flag
				}
			}
			else = {
				random_owned_state = {
					limit = {
						is_fully_controlled_by = ROOT
            			free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = yes
						}
						NOT = { has_state_flag = war_building_slot_flag }
						infrastructure > 4
					}
					add_extra_state_shared_building_slots = 1
					set_state_flag = war_building_slot_flag
				}
			}
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 10
				all_owned_state = { free_building_slots = { building = industrial_complex size < 1 include_locked = no } }
			}
			modifier = {
				any_owned_state = {
					infrastructure > 3
					free_building_slots = { building = industrial_complex size > 0 include_locked = no }
				}
				factor = 0
			}
		}
    }
}

propaganda_efforts = {
	war_propaganda = { 

		icon = generic_army_3

		available = {
			NOT = { has_country_flag = war_propaganda_campaign_running }
			has_war_support < 0.5
		}

		visible = {
			OR = {
				has_war_with_non_exiled_country = yes  
				any_neighbor_country = {
					AND = {
						NOT = { has_government = ROOT }
						has_offensive_war = yes
						has_added_tension_amount > 10
					}
				}
			}
		}

		fire_only_once = no

		days_remove = 90
		days_re_enable = 180

		cost = 150

		modifier = {
			war_support_weekly = 0.005
		}

		complete_effect = {
			set_country_flag = war_propaganda_campaign_running
		}
		remove_effect = {
			clr_country_flag = war_propaganda_campaign_running
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_war_support < 0.4
				has_war = yes
			}
		}
	}
	
	war_propaganda_casualties = { 

		icon = generic_army_3

		available = {
			#NOT = { has_country_flag = war_propaganda_campaign_running }
			has_casualties_war_support < 0
		}

		visible = {
			# has_war = yes
			has_war_with_non_exiled_country = yes
		}

		fire_only_once = no

		days_remove = 180
		
		remove_trigger = {
			has_war = no
        }
		
		cost = 100

		modifier = {
			weekly_casualties_war_support = 0.006
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 10 #AI should probably just choose this one for general purposes.
				has_war = yes
				has_war_support < 0.7
			}
			modifier = {
				add = 20
				has_war = yes
				has_war_support < 0.5
			}
			modifier = {
				add = 30
				has_war = yes
				has_war_support < 0.3
			}
			modifier = {
				add = 201 #ie; consider prio idea
				has_war = yes
				has_casualties_war_support < -0.25
			}
			modifier = {
				factor = 0
				has_manpower < 10000
				has_political_power < 250
			}
		}
	}
	
	war_propaganda_convoys = { 

		icon = GFX_decision_generic_naval

		available = {
			#NOT = { has_country_flag = war_propaganda_campaign_running }
			has_convoys_war_support < 0
		}

		visible = {
			# has_war = yes
			has_war_with_non_exiled_country = yes
		}

		fire_only_once = no

		days_remove = 180
		
		remove_trigger = {
			has_war = no
        }
		
		cost = 100

		modifier = {
			weekly_convoys_war_support = 0.006
		}

		#complete_effect = {
		#	set_country_flag = war_propaganda_campaign_running
		#}
		#remove_effect = {
		#	clr_country_flag = war_propaganda_campaign_running
		#}

		ai_will_do = {
			base = 0
			modifier = {
				add = 201 #ie; consider prio idea
				has_war = yes
				has_convoys_war_support < -0.29
			}
			modifier = {
				factor = 0
				has_manpower < 10000
				has_political_power < 250
			}
		}
	}


}

special_projects = {
	focus_explosives_production_on = {
		icon = GFX_decision_evil_web

        allowed = {
            always = yes
        }

        available = {
            has_war = yes
        }
        visible = {
            has_tech = advanced_rocket_engines
        }
        cost = 100
        days_remove = -1
        remove_trigger = {
        	OR = {
            	has_war = no
            	has_country_flag = focus_explosives_production_off_flag
            }
        }
        modifier = {
            nuclear_production_factor = 1.0
			research_speed_factor = -0.2
        }
        ai_will_do = {
			factor = 0
		}
		complete_effect = {
			set_country_flag = focus_explosives_production_on_flag
		}
		remove_effect = {
			clr_country_flag = focus_explosives_production_on_flag
			clr_country_flag = focus_explosives_production_off_flag
		}
	}

	focus_explosives_production_off = {
		icon = GFX_decision_unknown

        allowed = {
            always = yes
        }

        available = {
            always = yes
        }
        visible = {
            has_country_flag = focus_explosives_production_on_flag
        }
        cost = 50
        ai_will_do = {
			factor = 0
		}
		complete_effect = {
			effect_tooltip = {
				remove_decision = focus_explosives_production_on
			}
			set_country_flag = focus_explosives_production_off_flag
		}
	}
}

war_measures = {
	emergency_factory_conversion_defensive = {

		icon = generic_2

		available = {
			num_of_civilian_factories > 5 #Change this if amount of factories change
		}

		visible = {
			has_defensive_war = yes
			has_offensive_war = no			
		}

		fire_only_once = no

		days_re_enable = 90

		cost = 100

		complete_effect = {
			replace_civ_with_arms_factories = yes
			add_war_support = -0.05
		}
		ai_will_do = {
			factor = 0
		}
	}

	emergency_factory_conversion_offensive = {

		icon = generic_2

		available = {
			num_of_civilian_factories > 5 #Change this if amount of factories change
		}

		visible = {
			has_offensive_war = yes
		}

		fire_only_once = no

		days_re_enable = 90

		cost = 100

		complete_effect = {
			replace_civ_with_arms_factories = yes
			add_war_support = -0.05
			add_stability = -0.03
		}
		ai_will_do = {
			factor = 0
		}
	}

	desperate_defense = {

		icon = generic_army_3

		available = {
			surrender_progress > 0.5
		}

		visible = {
			surrender_progress > 0
		}

		fire_only_once = no

		days_remove = 45
		days_re_enable = 360

		cost = 50
		ai_will_do = {
			factor = 1
			modifier = {
				surrender_progress > 0.6
				factor = 10
			}
		}

		modifier = {
			defence = 0.2
			dig_in_speed_factor = 0.5
		}

		complete_effect = {

		}
	}
}

foreign_politics = {
	object_to_attaches = {

		allowed = {
            always = no # NOTE: I've disabled this decision because it seems to not be used a lot and be mostly clutter. - H
        }

        cost = 50
        fire_only_once = no

        days_re_enable = 30

        available = {
        }

        visible = {
        	FROM = {
        		NOT = { has_country_flag = rejected_withdrawing_attache }
        		NOT = { has_country_flag = rejected_withdrawing_attache@ROOT }
        		NOT = { has_country_flag = object_attache_going_on }
        	}
        	has_war = yes
        	has_capitulated = no
        	NOT = { has_war_with = FROM }
        	any_enemy_country = {
				has_attache_from = FROM
			}
		}

		target_root_trigger = {
			has_war = yes
			has_capitulated = no
		}

		target_trigger = {
			NOT = { has_war_with = FROM }
			#FROM = { has_sent_attache = yes } # Make such trigger to optimize if it becomes a performance problem again.
			any_enemy_country = {
				has_attache_from = FROM
			}			
		}

        complete_effect = {
			FROM = { country_event = { id = generic.14 hours = 6 } }
			hidden_effect = {
				FROM = { set_country_flag = object_attache_going_on }
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
}

foreign_support = {

}

operations = {
	
}

