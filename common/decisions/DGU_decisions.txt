DGU_saurons_schedule = {


	example_saurons_schedule_1_task_1 = {
		icon = generic_research # TODO

		days_mission_timeout = 100 # time to complete task

		fire_only_once = yes
		selectable_mission = no
		is_good = no

		allowed = {
			always = no
		}

		activation = {
			always = no
		}

		available = {
			always = no
			# TODO: add completion condition
		}
		complete_effect = {
			# TODO: reward when completed
			set_country_flag = schedule_1_task_1_completed
		}

		cancel_trigger = {
			MOR = {
				OR = {
					is_subject = yes
					exists = no
					has_capitulated = yes
					strength_ratio = {
						tag = DGU
						ratio < 0.25
					}
				}
			}
		}
		cancel_effect = {
			dgu_schedule_end = yes
		}

		timeout_effect = {
			# when the timer runs out uninterrupted
			hidden_effect = {
				dgu_schedule_end = yes
				MOR = { country_event = { id = dolguldur.9 days = 1 } }
			}
			event_option_tooltip = { text = dolguldur.9.a
		}
	}


}