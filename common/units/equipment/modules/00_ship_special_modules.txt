equipment_modules = {

	boarding_crew_module = {
		abbreviation = "cha"
		category = ship_special
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 1
			fuel_consumption = 2
			maximum_speed = 0.5
		}
		multiply_stats = {
			maximum_speed = 0.15
		}
	}

	ballista_module = {
		abbreviation = "cha"
		category = ship_special
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 1
			fuel_consumption = 2
			maximum_speed = 0.5
		}
		multiply_stats = {
			maximum_speed = 0.15
		}
	}

	windlance_module = {
		abbreviation = "cha"
		category = ship_special
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 1
			fuel_consumption = 2
			maximum_speed = 0.5
		}
		multiply_stats = {
			maximum_speed = 0.15
		}
	}

	mounted_catapult_module = {
		abbreviation = "cha"
		category = ship_special
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 1
			fuel_consumption = 2
			maximum_speed = 0.5
		}
		multiply_stats = {
			maximum_speed = 0.15
		}
	}

	expanded_food_stores_module = {
		abbreviation = "cha"
		category = ship_special
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 1
			fuel_consumption = 2
			maximum_speed = 0.5
		}
		multiply_stats = {
			maximum_speed = 0.15
		}
	}

	crows_nest_module = { # Lookout tower/mast
		abbreviation = "cha"
		category = ship_special
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 1
			fuel_consumption = 2
			maximum_speed = 0.5
		}
		multiply_stats = {
			maximum_speed = 0.15
		}
	}
}
