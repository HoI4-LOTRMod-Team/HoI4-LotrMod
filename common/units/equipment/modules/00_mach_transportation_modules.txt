equipment_modules = {
	limit = {
		has_dlc = "No Step Back" 
	}

	carried_module = {
		abbreviation = "cha"
		category = mach_transportation
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			
		}
		multiply_stats = {
			
		}
	}

	wooden_wheels_module = {
		abbreviation = "cha"
		category = mach_transportation
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 1.5
		}
		multiply_stats = {
			maximum_speed = 0.2
		}
	}

	wood_and_iron_wheels_module = {
		abbreviation = "cha"
		category = mach_transportation
		sfx = sfx_ui_sd_module_turret

		add_stats = {
			build_cost_ic = 3
			reliability = 0.05
		}
		multiply_stats = {
			maximum_speed = 0.3
		}
	}
}
