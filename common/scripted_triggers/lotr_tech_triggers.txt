


cavalry_designer_is_enabled = {
	has_dlc = "No Step Back"
	has_game_rule = {
		rule = RULE_CAV_DESIGNER 
		option = ENABLED
	}
}

siege_machine_designer_is_enabled = {
	has_dlc = "No Step Back"
	has_game_rule = {
		rule = RULE_MACH_DESIGNER 
		option = ENABLED
	}
}

ship_designer_is_enabled = {
	has_dlc = "Man the Guns"
	has_game_rule = {
		rule = RULE_SHIP_DESIGNER 
		option = ENABLED
	}
}

eagle_designer_is_enabled = {
	has_dlc = "By Blood Alone"
	has_game_rule = {
		rule = RULE_EAG_DESIGNER 
		option = ENABLED
	}
}

trebuchet_is_railgun_enabled = {
	has_dlc = "No Step Back"
	has_game_rule = {
		rule = RULE_TREBUCHET_RAILGUN
		option = ENABLED
	}
}