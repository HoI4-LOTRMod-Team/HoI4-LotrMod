



# if this COuntry has cfm active
has_cfm_active = {
	has_country_flag = cfm_active

	exists = yes
	has_capitulated = no
	NOT = { any_enemy_country = { has_country_flag = cfm_active } }

	# TODO... ? allied etc
	# Mirkwood also needs to exist and stuff (on action runs on them)
}

has_100_presence = {
	check_variable = {
		var = cfm_presence
		value = 100
		compare = greater_than_or_equals
	}
}