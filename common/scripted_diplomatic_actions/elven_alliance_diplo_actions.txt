scripted_diplomatic_actions = {
	
	invite_to_elven_assembly = {
		icon = 16		#	The icon to use for the dipomatic request, visible when the diplomatic action is sent, not visible if the diplomatic request does not require acceptance, must be included in the \gfx\interface\alerts\global_diplorequest_icons.dds file, number represents icon location from left to right.
		
		#### CONDITIONS TO USE/SEE ACTION ###
		
		allowed = {
			always = yes
		}
		visible = {
			FROM = { NOT = { is_elven_alliance_member = yes } }
			ROOT = { is_elven_alliance_member = yes }
		}
		selectable = {
			ROOT = { can_invite_to_elven_alliance = yes }

			FROM = { can_be_invited_to_elven_alliance = yes }
			FROM = { NOT = { has_war_with = ROOT } }
			FROM = { NOT = { is_sanctioned_by_elven_alliance = yes } }
			FROM = { is_not_at_war_with_elven_alliance = yes }

			NOT = { FROM = { has_country_flag = ea_has_pending_invitation } }
		}
		can_be_accepted = {
			always = yes
		}
		can_be_sent = {
			always = yes
		}
		
		cost = 50

		requires_acceptance = yes
		show_acceptance_on_action_button = yes	

		#### ACTION LOCALISATION ###

		send_description = invite_to_elven_assembly_send_desc
		receive_description = invite_to_elven_assembly_receive_desc
		accept_title = invite_to_elven_assembly_accept_title
		accept_description = invite_to_elven_assembly_accept_desc
		reject_title = invite_to_elven_assembly_reject_title
		reject_description = invite_to_elven_assembly_reject_desc
		#### EFFECTS ###		

		on_sent_effect = {		#	An effect that can be applied when sending the diplomatic action.
			set_country_flag = ea_has_pending_invitation
		}
		
		complete_effect = {		#	An effect that can be applied the action is ACCEPTED (accepted always if requires_acceptance is set to NO)
			if = { limit = { is_elven_alliance_member = no }
				elven_alliance_join = yes
			}
			clr_country_flag = ea_has_pending_invitation
		}
		reject_effect = {		#	An effect that can be applied the action is REJECTED (only valid if requires_acceptance is set to YES )
			set_country_flag = ea_has_declined_invitation
			clr_country_flag = ea_has_pending_invitation
		}
		
		#### AI SECTION ###
		
		ai_desire = {		# AI modifiers to use this diplomatic action
			modifier = {
				add = 20
				FROM = { is_ai = no }
			}
			modifier = {
				add = 10
				FROM = { is_same_race = yes }
			}
			modifier = {
				add = -25
				FROM = { has_same_ideology = no }
			}
			modifier = {
				add = 20
				is_ally_with = FROM
			}
			modifier = {
				factor=0
				FROM = { has_country_flag = ea_has_declined_invitation }
			}
		}
		ai_acceptance = {		# AI modifiers to accept this diplomatic action, not valid if requires_acceptance is set to NO.
			base_desire = {
				modifier = {
					factor = 10
				}
			}
		}
	}

	propose_kick_from_elven_assembly = {
		icon = 16		#	The icon to use for the dipomatic request, visible when the diplomatic action is sent, not visible if the diplomatic request does not require acceptance, must be included in the \gfx\interface\alerts\global_diplorequest_icons.dds file, number represents icon location from left to right.
		
		#### CONDITIONS TO USE/SEE ACTION ###
		
		allowed = {
			always = yes
		}
		visible = {
			is_elven_alliance_member = yes
			FROM = { is_elven_alliance_member = yes }
		}
		selectable = {
			ROOT = { can_propose_kick_elven_alliance = yes }

			NOT = { FROM = { has_country_flag = ea_last_kick_210_days } }
		}
		
		cost = 150

		requires_acceptance = no
		show_acceptance_on_action_button = yes	

		#### ACTION LOCALISATION ###

		send_description = propose_kick_from_elven_assembly_send_desc
		receive_description = propose_kick_from_elven_assembly_receive_desc
		#### EFFECTS ###		

		on_sent_effect = {		#	An effect that can be applied when sending the diplomatic action.
			set_country_flag = { flag = ea_last_kick_210_days days = 210 }
		}
		complete_effect = {		#	An effect that can be applied the action is ACCEPTED (accepted always if requires_acceptance is set to NO)
			
		}
		reject_effect = {		#	An effect that can be applied the action is REJECTED (only valid if requires_acceptance is set to YES )
		}
		
		#### AI SECTION ###
		
		ai_desire = {		# AI modifiers to use this diplomatic action
			modifier = {
				add = 10
			}
		}
		ai_acceptance = {		# AI modifiers to accept this diplomatic action, not valid if requires_acceptance is set to NO.
			base_desire = {
				modifier = {
					factor = 10
				}
			}		
		}
	}

	propose_sanction_elven_assembly = {
		icon = 16		#	The icon to use for the dipomatic request, visible when the diplomatic action is sent, not visible if the diplomatic request does not require acceptance, must be included in the \gfx\interface\alerts\global_diplorequest_icons.dds file, number represents icon location from left to right.
		
		#### CONDITIONS TO USE/SEE ACTION ###
		
		allowed = {
			always = yes
		}
		visible = {
			is_elven_alliance_member = yes
		}
		selectable = {
			can_invite_to_elven_alliance = yes
		}
		can_be_accepted = {
			always = yes
		}
		can_be_sent = {
			can_be_invited_to_elven_alliance = yes
		}
		
		cost = 150

		requires_acceptance = yes
		show_acceptance_on_action_button = yes	

		#### ACTION LOCALISATION ###

		send_description = invite_to_elven_assembly_send_desc
		receive_description = invite_to_elven_assembly_receive_desc
		accept_title = invite_to_elven_assembly_accept_title
		accept_description = invite_to_elven_assembly_accept_desc
		reject_title = invite_to_elven_assembly_reject_title
		reject_description = invite_to_elven_assembly_reject_desc
		#### EFFECTS ###		

		on_sent_effect = {		#	An effect that can be applied when sending the diplomatic action.
		}
		complete_effect = {		#	An effect that can be applied the action is ACCEPTED (accepted always if requires_acceptance is set to NO)
		}
		reject_effect = {		#	An effect that can be applied the action is REJECTED (only valid if requires_acceptance is set to YES )
		}
		
		#### AI SECTION ###
		
		ai_desire = {		# AI modifiers to use this diplomatic action
			modifier = {
				add = 10
			}
		}
		ai_acceptance = {		# AI modifiers to accept this diplomatic action, not valid if requires_acceptance is set to NO.
			base_desire = {
				modifier = {
					factor = 10
				}
			}		
		}
	}

	propose_joint_war_elven_assembly = {
		icon = 16		#	The icon to use for the dipomatic request, visible when the diplomatic action is sent, not visible if the diplomatic request does not require acceptance, must be included in the \gfx\interface\alerts\global_diplorequest_icons.dds file, number represents icon location from left to right.
		
		#### CONDITIONS TO USE/SEE ACTION ###
		
		allowed = {
			always = yes
		}
		visible = {
			is_elven_alliance_member = yes
		}
		selectable = {
			can_invite_to_elven_alliance = yes
		}
		can_be_accepted = {
			always = yes
		}
		can_be_sent = {
			can_be_invited_to_elven_alliance = yes
		}
		
		cost = 150

		requires_acceptance = yes
		show_acceptance_on_action_button = yes	

		#### ACTION LOCALISATION ###

		send_description = invite_to_elven_assembly_send_desc
		receive_description = invite_to_elven_assembly_receive_desc
		accept_title = invite_to_elven_assembly_accept_title
		accept_description = invite_to_elven_assembly_accept_desc
		reject_title = invite_to_elven_assembly_reject_title
		reject_description = invite_to_elven_assembly_reject_desc
		#### EFFECTS ###		

		on_sent_effect = {		#	An effect that can be applied when sending the diplomatic action.
		}
		complete_effect = {		#	An effect that can be applied the action is ACCEPTED (accepted always if requires_acceptance is set to NO)
		}
		reject_effect = {		#	An effect that can be applied the action is REJECTED (only valid if requires_acceptance is set to YES )
		}
		
		#### AI SECTION ###
		
		ai_desire = {		# AI modifiers to use this diplomatic action
			modifier = {
				add = 10
			}
		}
		ai_acceptance = {		# AI modifiers to accept this diplomatic action, not valid if requires_acceptance is set to NO.
			base_desire = {
				modifier = {
					factor = 10
				}
			}		
		}
	}

	propose_make_leader_elven_assembly = {
		icon = 16		#	The icon to use for the dipomatic request, visible when the diplomatic action is sent, not visible if the diplomatic request does not require acceptance, must be included in the \gfx\interface\alerts\global_diplorequest_icons.dds file, number represents icon location from left to right.
		
		#### CONDITIONS TO USE/SEE ACTION ###
		
		allowed = {
			always = yes
		}
		visible = {
			is_elven_alliance_member = yes
		}
		selectable = {
			can_invite_to_elven_alliance = yes
		}
		can_be_accepted = {
			always = yes
		}
		can_be_sent = {
			can_be_invited_to_elven_alliance = yes
		}
		
		cost = 150

		requires_acceptance = yes
		show_acceptance_on_action_button = yes	

		#### ACTION LOCALISATION ###

		send_description = invite_to_elven_assembly_send_desc
		receive_description = invite_to_elven_assembly_receive_desc
		accept_title = invite_to_elven_assembly_accept_title
		accept_description = invite_to_elven_assembly_accept_desc
		reject_title = invite_to_elven_assembly_reject_title
		reject_description = invite_to_elven_assembly_reject_desc
		#### EFFECTS ###		

		on_sent_effect = {		#	An effect that can be applied when sending the diplomatic action.
		}
		complete_effect = {		#	An effect that can be applied the action is ACCEPTED (accepted always if requires_acceptance is set to NO)
		}
		reject_effect = {		#	An effect that can be applied the action is REJECTED (only valid if requires_acceptance is set to YES )
		}
		
		#### AI SECTION ###
		
		ai_desire = {		# AI modifiers to use this diplomatic action
			modifier = {
				add = 10
			}
		}
		ai_acceptance = {		# AI modifiers to accept this diplomatic action, not valid if requires_acceptance is set to NO.
			base_desire = {
				modifier = {
					factor = 10
				}
			}		
		}
	}
}