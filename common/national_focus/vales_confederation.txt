#!gfx:interface\auto_generated\lotr_goals.gfx


    joint_focus = {
        id = VAL_confederation_of_the_anduin
        icon = GFX_goal_generic_unity
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 41
        y = 0

        cost = 10
        ai_will_do = { factor = 25 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
			add_political_power = 150
            hidden_effect = {
                if = {
                    limit = {
                        OR = { is_in_faction = yes any_country = { is_subject_of = ROOT } }
                    }
                    news_event = news.409
                }
            }
            
		}

	    completion_reward_joint_originator = {
		
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_rhosgobels_contribution
		prerequisite = { focus = VAL_free_river_trade }
        
        icon = GFX_goal_generic_befriend_rgl
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 37
        y = 4

        cost = 5
        ai_will_do = { factor = 12 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = RGL
                RGL = { all_core_state = { is_controlled_by = ROOT } }
                RGL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# opinion improvement with all  
		}

	    completion_reward_joint_originator = {
            random_core_state = {
                limit = { oil > 0 }
                add_resource = {
                    type = oil
                    amount = 5
                }
            }
            random_core_state = {
                limit = { chromium > 0 }
                add_resource = {
                    type = chromium
                    amount = 5
                }
            }
	    }

	    completion_reward_joint_member = {
            random_core_state = {
                limit = { oil > 0 }
                add_resource = {
                    type = oil
                    amount = 5
                }
            }
            random_core_state = {
                limit = { chromium > 0 }
                add_resource = {
                    type = chromium
                    amount = 5
                }
            }
	    }
    }

    joint_focus = {
        id = VAL_gladdens_contribution
		prerequisite = { focus = VAL_free_river_trade }
        
        icon = GFX_goal_generic_befriend_gld
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 45
        y = 4

        cost = 5
        ai_will_do = { factor = 12 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = GLD
                GLD = { all_core_state = { is_controlled_by = ROOT } }
                GLD = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# scouts and messengers
		}

	    completion_reward_joint_originator = {
            if = {
                limit = {
                    has_tech = tech_signal_company
                    has_tech = tech_recon
                }
                add_tech_bonus = {
                    name = special_bonus
                    category = lotr_support_tech
                    uses = 1
                    bonus = 0.25
                }
            }
            else = {
                if = {
                    limit = { NOT = { has_tech = tech_signal_company } }
                    set_technology = { tech_signal_company = 1 }
                }
                if = {
                    limit = { NOT = { has_tech = tech_recon } }
                    set_technology = { tech_recon = 1 }
                }
            }
	    }

	    completion_reward_joint_member = {
            if = {
                limit = {
                    has_tech = tech_signal_company
                    has_tech = tech_recon
                }
                add_tech_bonus = {
                    name = special_bonus
                    category = lotr_support_tech
                    uses = 1
                    bonus = 0.25
                }
            }
            else = {
                if = {
                    limit = { NOT = { has_tech = tech_signal_company } }
                    set_technology = { tech_signal_company = 1 }
                }
                if = {
                    limit = { NOT = { has_tech = tech_recon } }
                    set_technology = { tech_recon = 1 }
                }
            }
	    }
    }

    joint_focus = {
        id = VAL_beorning_contribution
		prerequisite = { focus = VAL_free_river_trade }
        
        icon = GFX_goal_generic_befriend_val
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 41
        y = 4

        cost = 5
        ai_will_do = { factor = 12 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = VAL
                VAL = { all_core_state = { is_controlled_by = ROOT } }
                VAL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# beorining ironwork
		}

	    completion_reward_joint_originator = {
            random_state_add_building_slot_and_mil_x2 = yes
		
	    }

	    completion_reward_joint_member = {
            random_state_add_building_slot_and_mil = yes
	    }
    }

    joint_focus = {
        id = VAL_brown_wizards_blessing
		prerequisite = { focus = VAL_rhosgobels_contribution }
        
        icon = GFX_goal_generic_radagast
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 36
        y = 5

        cost = 5
        ai_will_do = {
            factor = 3
            modifier = {
                add = 5
                GLD = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 5
                VAL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 3
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            RGL = { RGL_radagast = { has_advisor_role = chief_advisor } }
            OR = {
                tag = RGL
                RGL = { all_core_state = { is_controlled_by = ROOT } }
                RGL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# research or something  
		}

	    completion_reward_joint_originator = {
            add_tech_bonus = {
                name = special_bonus
                category = lotr_knowledge_magic
                bonus = 0.35
                uses = 1
            }
            add_tech_bonus = {
                name = special_bonus
                category = lotr_knowledge_alchemy_herbology
                bonus = 0.35
                uses = 1
            }
	    }

	    completion_reward_joint_member = {
            add_tech_bonus = {
                name = special_bonus
                category = lotr_knowledge_magic
                bonus = 0.35
                uses = 1
            }
	    }
    }

    joint_focus = {
        id = VAL_eyes_of_the_eyrie
		prerequisite = { focus = VAL_gladdens_contribution }
        
        icon = GFX_goal_MIR_scout_eagle_focus
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 44
        y = 5

        cost = 5
        ai_will_do = {
            factor = 3
            modifier = {
                add = 5
                RGL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 5
                VAL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 3
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
            modifier = {
                factor = 2
                any_country = {
                    is_vales_confederation_member = yes
                    target_air_focus = yes
                    is_ai = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = GLD
                GLD = { all_core_state = { is_controlled_by = ROOT } }
                GLD = { is_subject_of = ROOT }
            }
            OR = {
                GLD = { controls_state = 268 }
                ROOT = { controls_state = 268 }
            }
        }
        completion_reward = {
			# everyone gets some eagles  
		}

	    completion_reward_joint_originator = {
            if = {
                limit = { NOT = { has_tech = eagle_domestication_men } }
                set_technology = { eagle_domestication_men = 1 }
            }
            else_if = {
                limit = { NOT = { has_tech = fighter_eagle_men } }
                set_technology = { fighter_eagle_men = 1 }
            }
            else = {
                add_equipment_to_stockpile = {
                    type = fighter_eagle_airframe_0
                    amount = 5
                }
            }
	    }

	    completion_reward_joint_member = {
            if = {
                limit = { NOT = { has_tech = eagle_domestication_men } }
                set_technology = { eagle_domestication_men = 1 }
            }
            else_if = {
                limit = { NOT = { has_tech = fighter_eagle_men } }
                set_technology = { fighter_eagle_men = 1 }
            }
            else = {
                add_equipment_to_stockpile = {
                    type = fighter_eagle_airframe_0
                    amount = 5
                }
            }
	    }
    }

    joint_focus = {
        id = VAL_riders_of_the_iris
		prerequisite = { focus = VAL_gladdens_contribution }
        
        icon = GFX_goal_generic_lancer_cav_charge
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 46
        y = 5

        cost = 5
        ai_will_do = {
            factor = 3
            modifier = {
                add = 5
                RGL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 5
                VAL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 3
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
            modifier = {
                factor = 2
                any_country = {
                    is_vales_confederation_member = yes
                    target_any_cavalry_strat = yes
                    is_ai = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = GLD
                GLD = { all_core_state = { is_controlled_by = ROOT } }
                GLD = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# everyone gets some light cav  
		}

	    completion_reward_joint_originator = {
            if = {
                limit = { NOT = { has_tech = basic_cav } }
                set_technology = { basic_cav = 1 }
            }
            else = {
                add_equipment_to_stockpile = {
                    type = light_cavalry_chassis_0
                    amount = 120
                }
            }
		
	    }

	    completion_reward_joint_member = {
             if = {
                limit = { NOT = { has_tech = basic_cav } }
                set_technology = { basic_cav = 1 }
            }
            else = {
                add_equipment_to_stockpile = {
                    type = light_cavalry_chassis_0
                    amount = 120
                }
            }
	    }
    }

    joint_focus = {
        id = VAL_skin_changer_vanguards
		prerequisite = { focus = VAL_beorning_contribution }
        
        icon = GFX_goal_generic_beorning_bear_urs
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 40
        y = 5

        cost = 5
        ai_will_do = {
            factor = 2
            modifier = {
                add = 5
                RGL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 3
                GLD = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 3
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = VAL
                VAL = { all_core_state = { is_controlled_by = ROOT } }
                VAL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# everyone gets skin changer div  
		}

	    completion_reward_joint_originator = {
            add_unit_bonus = {
                skinchangers = {
                    soft_attack = 0.1
                    hard_attack = 0.1
                    defense = 0.1
                    armor_value = 0.35
                }
            }
	    }

	    completion_reward_joint_member = {
            set_technology = {
                skinchangers_tech = 1
            }
	    }
    }

    joint_focus = {
        id = VAL_honeycakes_and_hardiness
		prerequisite = { focus = VAL_beorning_contribution }
        
        icon = GFX_goal_VAL_honeycakes
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 42
        y = 5

        cost = 5
        ai_will_do = {
            factor = 3
            modifier = {
                add = 5
                RGL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 5
                VAL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 3
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = VAL
                VAL = { all_core_state = { is_controlled_by = ROOT } }
                VAL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# everyone gets idea  
		}

	    completion_reward_joint_originator = {
            add_stability = 0.05
            add_unit_bonus = {
                category_all_infantry = {
                    max_strength = 0.05
                }
            }
	    }

	    completion_reward_joint_member = {
            set_technology = {
                honeycakes = 1
            }
	    }
    }

    joint_focus = {
        id = VAL_marsh_strider_tactics
		prerequisite = { focus = VAL_rhosgobels_contribution }
        
        icon = GFX_goal_RHU_army_inf_marshes
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 38
        y = 5

        cost = 5
        ai_will_do = {
            factor = 5
            modifier = {
                add = 5
                RGL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 5
                VAL = { is_vales_confederation_member = yes }
            }
            modifier = {
                add = 3
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
            modifier = {
                factor = 2
                any_country = {
                    is_vales_confederation_member = yes
                    has_war = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = RGL
                RGL = { all_core_state = { is_controlled_by = ROOT } }
                RGL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# river crossing and marines  
		}

	    completion_reward_joint_originator = {
		    add_unit_bonus = {
                category_all_infantry = {
                    marsh = {
                        attack = 0.05
                        defence = 0.05
                        movement = 0.1
                    }
                    river = {
                        attack = 0.05
                        defence = 0.05
                        movement = 0.1
                    }
                }
            }
            custom_effect_tooltip = VAL_marsh_strider_tactics_bonus_tt
	    }

	    completion_reward_joint_member = {
            add_unit_bonus = {
                category_all_infantry = {
                    marsh = {
                        attack = 0.05
                        defence = 0.05
                        movement = 0.1
                    }
                    river = {
                        attack = 0.05
                        defence = 0.05
                        movement = 0.1
                    }
                }
            }
	    }
    }

    joint_focus = {
        id = VAL_integrate_gladden
		prerequisite = { focus = VAL_eyes_of_the_eyrie }
        prerequisite = { focus = VAL_riders_of_the_iris }
        
        icon = GFX_goal_VAL_integrate_gld
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 45
        y = 6

        cost = 10
        ai_will_do = {
            factor = 25
            modifier = {
                factor = 0.2
                has_country_flag = rng_30p_flag_3
            }
            modifier = {
                factor = 0
                GLD = { is_ai = no }
                is_ally_with = GLD
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = GLD
                GLD = { all_core_state = { is_controlled_by = ROOT } }
                GLD = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# everyone gains stuff, if occupied, gain core  
            if = {
                limit = { NOT = { has_completed_focus = VAL_free_river_trade } }
                GLD = {
                    custom_effect_tooltip = VAL_integration_effect_tt_1
                    custom_effect_tooltip = VAL_integration_effect_tt_2
                    custom_effect_tooltip = VAL_integration_effect_tt_3
                }
            }
            # if GLD is annexed -> gain cores
            else_if = {
                limit = { GLD = { exists = no } }
                GLD = {
                    every_core_state = {
                        limit = { controller = { is_vales_confederation_member = yes } }
                        add_core_of = controller
                    }
                }
            }
            # if GLD is puppet -> annex
            else_if = {
                limit = { GLD = { is_subject = yes overlord = { is_vales_confederation_member = yes } } }
                GLD = {
                    OVERLORD = {
                        annex_country = {
                            target = GLD
                        }
                    }
                }
            }
            # if GLD is allied -> annex
            else_if = {
                limit = { GLD = { is_in_faction = yes faction_leader = { is_vales_confederation_member = yes } } }
                GLD = {
                    faction_leader = {
                        set_autonomy = {
                            target = GLD
                            autonomy_state = autonomy_personal_union
                        }
                    }
                }
            }
		}

	    completion_reward_joint_originator = {
            
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_integrate_the_beornings
		prerequisite = { focus = VAL_skin_changer_vanguards }
        prerequisite = { focus = VAL_honeycakes_and_hardiness }
        
        icon = GFX_goal_VAL_integrate_val
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 41
        y = 6

        cost = 10
        ai_will_do = {
            factor = 25
            modifier = {
                factor = 0.2
                has_country_flag = rng_30p_flag_3
            }
            modifier = {
                factor = 0
                VAL = { is_ai = no }
                is_ally_with = VAL
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = VAL
                VAL = { all_core_state = { is_controlled_by = ROOT } }
                VAL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# everyone gains stuff, if occupied, gain core  
            if = {
                limit = { NOT = { has_completed_focus = VAL_free_river_trade } }
                VAL = {
                    custom_effect_tooltip = VAL_integration_effect_tt_1
                    custom_effect_tooltip = VAL_integration_effect_tt_2
                    custom_effect_tooltip = VAL_integration_effect_tt_3
                }
            }
            # if VAL is annexed -> gain cores
            else_if = {
                limit = { VAL = { exists = no } }
                VAL = {
                    every_core_state = {
                        limit = { controller = { is_vales_confederation_member = yes } }
                        add_core_of = controller
                    }
                }
            }
            # if VAL is puppet -> annex
            else_if = {
                limit = { VAL = { is_subject = yes overlord = { is_vales_confederation_member = yes } } }
                VAL = {
                    OVERLORD = {
                        annex_country = {
                            target = VAL
                        }
                    }
                }
            }
            # if VAL is allied -> annex
            else_if = {
                limit = { VAL = { is_in_faction = yes faction_leader = { is_vales_confederation_member = yes } } }
                VAL = {
                    faction_leader = {
                        set_autonomy = {
                            target = VAL
                            autonomy_state = autonomy_personal_union
                        }
                    }
                }
            }
		}

	    completion_reward_joint_originator = {
		
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_integrate_rhosgobel
		prerequisite = { focus = VAL_brown_wizards_blessing }
        prerequisite = { focus = VAL_marsh_strider_tactics }
        
        icon = GFX_goal_VAL_integrate_rgl
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 37
        y = 6

        cost = 10
        ai_will_do = {
            factor = 25
            modifier = {
                factor = 0.2
                has_country_flag = rng_30p_flag_3
            }
            modifier = {
                factor = 0
                RGL = { is_ai = no }
                is_ally_with = RGL
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            OR = {
                tag = RGL
                RGL = { all_core_state = { is_controlled_by = ROOT } }
                RGL = { is_subject_of = ROOT }
            }
        }
        completion_reward = {
			# everyone gains stuff, if occupied, gain core  
            if = {
                limit = { NOT = { has_completed_focus = VAL_free_river_trade } }
                RGL = {
                    custom_effect_tooltip = VAL_integration_effect_tt_1
                    custom_effect_tooltip = VAL_integration_effect_tt_2
                    custom_effect_tooltip = VAL_integration_effect_tt_3
                }
            }
            # if RGL is annexed -> gain cores
            else_if = {
                limit = { RGL = { exists = no } }
                RGL = {
                    every_core_state = {
                        limit = { controller = { is_vales_confederation_member = yes } }
                        add_core_of = controller
                    }
                }
            }
            # if RGL is puppet -> annex
            else_if = {
                limit = { RGL = { is_subject = yes overlord = { is_vales_confederation_member = yes } } }
                RGL = {
                    OVERLORD = {
                        annex_country = {
                            target = RGL
                        }
                    }
                }
            }
            # if RGL is allied -> annex
            else_if = {
                limit = { RGL = { is_in_faction = yes faction_leader = { is_vales_confederation_member = yes } } }
                RGL = {
                    faction_leader = {
                        set_autonomy = {
                            target = RGL
                            autonomy_state = autonomy_personal_union
                        }
                    }
                }
            }
		}

	    completion_reward_joint_originator = {
		
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_unified_records
		prerequisite = { focus = VAL_confederation_of_the_anduin }
        
        icon = GFX_goal_generic_tome_book
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 38
        y = 1

        cost = 10
        ai_will_do = {
            factor = 18
            modifier = {
                add = 8
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
			# stability  
		}

	    completion_reward_joint_originator = {
            add_ideas = VAL_unified_records
	    }

	    completion_reward_joint_member = {
            add_ideas = VAL_unified_records
	    }
    }

    joint_focus = {
        id = VAL_coordinated_planning
		prerequisite = { focus = VAL_confederation_of_the_anduin }
        
        icon = GFX_goal_VAL_river_battle_planning
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 45
        y = 1

        cost = 10
        ai_will_do = {
            factor = 5
            modifier = {
                factor = 2
                threat > 0.5
            }
            modifier = {
                factor = 0.2
                any_country = {
                    is_vales_confederation_member = yes
                    has_war = yes
                }
            }
            modifier = {
                factor = 0.1
                has_war = yes
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
            
        }

	    completion_reward_joint_originator = {
            add_ideas = generic_military_planning
	    }

	    completion_reward_joint_member = {
            add_ideas = generic_military_planning
	    }
    }

    joint_focus = {
        id = VAL_defensive_posture
		prerequisite = { focus = VAL_coordinated_planning }

        mutually_exclusive = {
            focus = VAL_deal_with_dol_guldur
            focus = VAL_deal_with_goblin_town
            focus = VAL_deal_with_gundabad
        }
        
        icon = GFX_goal_generic_defensive_fort
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 42
        y = 2

        cost = 5
        ai_will_do = { factor = 15 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
			# defensive idea  
            
		}

	    completion_reward_joint_originator = {
            add_ideas = defense_focus
	    }

	    completion_reward_joint_member = {
            add_ideas = defense_focus
	    }
    }

    joint_focus = {
        id = VAL_deal_with_dol_guldur
		prerequisite = { focus = VAL_coordinated_planning }
        
        icon = GFX_goal_goal_generic_major_war
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }

        mutually_exclusive = {
            focus = VAL_defensive_posture
            focus = VAL_deal_with_goblin_town
            focus = VAL_deal_with_gundabad
        }
        
        x = 44
        y = 2

        cost = 5
        ai_will_do = { factor = 5 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            all_country = {
                OR = {
                    is_vales_confederation_member = no
                    DGU = { war_with_is_possible = yes }
                }
            }
            OR = {
                has_government = belligerent
                has_government = revolutionary
                DGU = {
                    has_same_ideology = no
                    has_added_tension_amount > 5
                }
            }
            is_subject = no
        }
        completion_reward = {
			# war against DGU  
		}

	    completion_reward_joint_originator = {
            create_wargoal = {
                target = DGU
                type = puppet_wargoal_focus
            }
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_deal_with_goblin_town
		prerequisite = { focus = VAL_coordinated_planning }
        
        icon = GFX_goal_goal_generic_major_war
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }

        mutually_exclusive = {
            focus = VAL_defensive_posture
            focus = VAL_deal_with_dol_guldur
            focus = VAL_deal_with_gundabad
        }
        
        x = 46
        y = 2

        cost = 5
        ai_will_do = { factor = 5 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            all_country = {
                OR = {
                    is_vales_confederation_member = no
                    GOB = { war_with_is_possible = yes }
                }
            }
            OR = {
                has_government = belligerent
                has_government = revolutionary
                GOB = {
                    has_same_ideology = no
                    has_added_tension_amount > 5
                }
            }
            is_subject = no
        }
        completion_reward = {
			# war against GOB  
		}

	    completion_reward_joint_originator = {
            create_wargoal = {
                target = GOB
                type = puppet_wargoal_focus
            }
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_contact_the_hill_men
		prerequisite = { focus = VAL_deal_with_goblin_town }
        
        icon = GFX_goal_ROH_cross_mountain_diplomacy_3
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 46
        y = 3

        cost = 5
        ai_will_do = { factor = 18 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            all_country = {
                OR = {
                    is_vales_confederation_member = no
                    RHD = { can_engage_in_diplo_with = yes }
                }
            }
        }
        completion_reward = {
			# send envoy to RHD
		}

	    completion_reward_joint_originator = {
            RHD = { send_delegation_to_country = yes }
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_deal_with_gundabad
		prerequisite = { focus = VAL_coordinated_planning }
        
        icon = GFX_goal_goal_generic_major_war
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }

        mutually_exclusive = {
            focus = VAL_defensive_posture
            focus = VAL_deal_with_goblin_town
            focus = VAL_deal_with_dol_guldur
        }
        
        x = 48
        y = 2

        cost = 5
        ai_will_do = { factor = 5 }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
            all_country = {
                OR = {
                    is_vales_confederation_member = no
                    GUN = { war_with_is_possible = yes }
                }
            }
            OR = {
                has_government = belligerent
                has_government = revolutionary
                GUN = {
                    has_same_ideology = no
                    has_added_tension_amount > 5
                }
            }
            is_subject = no
        }
        completion_reward = {
			# war against GUN  
		}

	    completion_reward_joint_originator = {
            create_wargoal = {
                target = GUN
                type = puppet_wargoal_focus
            }
	    }

	    completion_reward_joint_member = {
		
	    }
    }

    joint_focus = {
        id = VAL_free_river_trade
		prerequisite = { focus = VAL_confederation_of_the_anduin }
        
        icon = GFX_goal_VAL_river_markets_2
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 41
        y = 3

        cost = 5
        ai_will_do = {
            factor = 5
            modifier = {
                factor = 1.5
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
			custom_effect_tooltip = VAL_free_river_trade_tt
		}

	    completion_reward_joint_originator = {
            hidden_effect = {
                if = {
                    limit = { NOT = { tag = RGL } RGL = { is_vales_confederation_member = yes } }
                    RGL = { create_free_trade_with = yes }
                }
                if = {
                    limit = { NOT = { tag = GLD } GLD = { is_vales_confederation_member = yes } }
                    GLD = { create_free_trade_with = yes }
                }
                if = {
                    limit = { NOT = { tag = VAL } VAL = { is_vales_confederation_member = yes } }
                    VAL = { create_free_trade_with = yes }
                }
            }
	    }

	    completion_reward_joint_member = {
            hidden_effect = {
                if = {
                    limit = { NOT = { tag = RGL } RGL = { is_vales_confederation_member = yes } }
                    RGL = { create_free_trade_with = yes }
                }
                if = {
                    limit = { NOT = { tag = GLD } GLD = { is_vales_confederation_member = yes } }
                    GLD = { create_free_trade_with = yes }
                }
                if = {
                    limit = { NOT = { tag = VAL } VAL = { is_vales_confederation_member = yes } }
                    VAL = { create_free_trade_with = yes }
                }
            }
	    }
    }

    joint_focus = {
        id = VAL_unified_tactics
		prerequisite = { focus = VAL_unified_records }
        
        icon = GFX_goal_goal_generic_air_doctrine
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 40
        y = 2

        cost = 5
        ai_will_do = {
            factor = 5
            modifier = {
                factor = 1.5
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
			# army xp and mastery  
		}

	    completion_reward_joint_originator = {
            army_experience = 50
            add_mastery = {
                amount = 150
                folder = land
            }
	    }

	    completion_reward_joint_member = {
            army_experience = 50
            add_mastery = {
                amount = 150
                folder = land
            }
	    }
    }

    joint_focus = {
        id = VAL_migration_treaties
		prerequisite = { focus = VAL_unified_records }
        
        icon = GFX_goal_generic_refugees_peasants
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 36
        y = 2

        cost = 10
        ai_will_do = {
            factor = 5
            modifier = {
                factor = 1.5
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
            modifier = {
                factor = 2
                has_manpower < 5000
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
			# manpower for all  
		}

	    completion_reward_joint_originator = {
            add_ideas = VAL_migration_treaties
	    }

	    completion_reward_joint_member = {
            add_ideas = VAL_migration_treaties
	    }
    }

    joint_focus = {
        id = VAL_knowledge_sharing
		prerequisite = { focus = VAL_unified_records }
        
        icon = GFX_goal_ROH_technological_coherence_research_flask_vial
        text_icon = joint_focus_style

        joint_trigger = {
            is_vales_confederation_member = yes
        }
        
        x = 38
        y = 2

        cost = 10
        ai_will_do = {
            factor = 10
            modifier = {
                factor = 1.8
                any_country = {
                    is_ai = no
                    is_vales_confederation_member = yes
                }
            }
        }
        
        search_filters = { FOCUS_FILTER_VALES_CONFEDERATION }
        available = {
            is_vales_confederation_member = yes
        }
        completion_reward = {
			# research sharing group  
		}

	    completion_reward_joint_originator = {
            add_to_tech_sharing_group = confederation_of_the_anduin
	    }

	    completion_reward_joint_member = {
            add_to_tech_sharing_group = confederation_of_the_anduin
	    }
    }

