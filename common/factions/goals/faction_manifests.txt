@manifest_fulfilled_value = 0.75

# Defense of Cooperative 
faction_manifest_defense_of_cooperative = {
	name = defense_of_cooperative_goal_name
	description = defense_of_cooperative_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = 10
		completed_amount_collection = collection:world_uncapitulated_cooperative_countries_and_fiefdom
			
		scale = {
			trade_opinion_factor = 0.20
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					guarantee_cost = -0.50
				}
			}
		}
	}
}

# Containment of Revolutionaries
faction_manifest_containment_of_revolutionary = {
	name = containment_of_revolutionary_goal_name
	description = containment_of_revolutionary_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_revolutionary_states
		
		scale = {
			revolutionary_drift = -0.08
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					required_garrison_factor = -0.15
				}
			}
		}
	}
}

# Defeat of Belligerent nations
faction_manifest_defeat_belligerent_nations = {
	name = defeat_belligerent_nations_goal_name
	description = defeat_belligerent_nations_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:world_non_belligerent_controlled_states

		range = { 
			max = 1
		}
		
		scale = {
			party_popularity_stability_factor = 0.10
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					monthly_population = 0.15
				}
			}
		}
	}
}

# Conquest of territory 
faction_manifest_conquest_of_territory = {
	name = conquest_of_territory_goal_name
	description = conquest_of_territory_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:faction_owned_core_states
		completed_amount_collection = collection:faction_controlled_non_core_states
		range = { 
			max = 0.75
		}
		
		scale = {
			offensive_war_stability_factor = 0.15
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					non_core_manpower = 0.03
				}			
			}
		}
	}
}


# Spread of revolutionary 
faction_manifest_spread_of_revolutionary = {
	name = spread_of_revolutionary_goal_name
	description = spread_of_revolutionary_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_countries
		completed_amount_collection = collection:world_revolutionary_countries
		range = { 
			max = 1
		}
		
		scale = {
			revolutionary_drift = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					conscription_factor = 0.05
					party_popularity_stability_factor = 0.1
				}			
			}
		}
	}

}

# Strength in Unity 
faction_manifest_strength_in_unity = {
	name = strength_in_unity_goal_name
	description = strength_in_unity_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = 8
		completed_amount_collection = {
			input = game:scope
			operators = { faction_members }
			name = COLLECTION_FACTION_MEMBERS
		}
		
		scale = {
			trade_opinion_factor = 0.20
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					political_power_factor = 0.1
				}			
			}
		}
	}
}

# Protector of Peace
faction_manifest_protector_of_peace = {
	name = protector_of_peace_goal_name
	description = protector_of_peace_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_countries
		completed_amount_collection = collection:world_at_peace_countries
		
		scale = {
			production_speed_buildings_factor = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					trade_opinion_factor = 0.2
				}			
			}
		}
	}
}


# Strategic Resource Control
faction_manifest_strategic_resource_control = {
	name = strategic_resource_control_goal_name
	description = strategic_resource_control_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount = global.global_resource_extracted_total # All the units in the world
		completed_amount = ROOT.faction_resource_extracted_total # resources controlled by Faction Members

		range = { 
			max = 0.30 # goal is to have at least 30% 
		}
		
		scale = {
			industrial_capacity_dockyard = 0.1
			industrial_capacity_factory = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = { 
					production_speed_buildings_factor = 0.1
				}
			}
		}
	}
}


