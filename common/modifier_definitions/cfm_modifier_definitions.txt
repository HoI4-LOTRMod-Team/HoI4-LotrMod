# modifiers used in static_modifiers needs to be predefined here

cfm_presence_gain = {
	color_type = good
	value_type = number
	precision = 2

	category = country
}

cfm_presence_gain_factor = {
	color_type = good
	value_type = percentage
	precision = 0

	category = country
}