


food_use_from_population_factor = {
	color_type = bad
	value_type = percentage
	precision = 3

	category = country
}

extra_military_factories = {
	color_type = good
	value_type = number
	precision = 0

	category = country
}

extra_civilian_factories = {
	color_type = good
	value_type = number
	precision = 0

	category = country
}

extra_military_factories_fake = { # Doesn't do anything but can be used for display
	color_type = good
	value_type = number
	precision = 0

	category = country
}

extra_civilian_factories_fake = { # Doesn't do anything but can be used for display
	color_type = good
	value_type = number
	precision = 0

	category = country
}

fellowship_detection_factor = {
	color_type = good
	value_type = percentage
	precision = 3

	category = country
}