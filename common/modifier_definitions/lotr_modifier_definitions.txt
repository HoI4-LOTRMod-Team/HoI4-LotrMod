


food_use_from_population_factor = {
	color_type = bad
	value_type = percentage
	precision = 3

	category = country
}

extra_military_factories = { # NOTE: This has been temporarily disabled for performancve because it isnt used by anyone, to enable, un-comment the three "building_modifiers_apply" lines in lotr_globals on actions file
	color_type = good
	value_type = number
	precision = 0

	category = country
}

extra_civilian_factories = { # NOTE: This has been temporarily disabled for performancve because it isnt used by anyone, to enable, un-comment the three "building_modifiers_apply" lines in lotr_globals on actions file. But it DOES work, and isnt a huge performance hog
	color_type = good
	value_type = number
	precision = 0

	category = country
}

extra_military_factories_fake = { # Doesn't do anything but can be used for display
	color_type = good
	value_type = number
	precision = 0

	category = country
}

extra_civilian_factories_fake = { # Doesn't do anything but can be used for display
	color_type = good
	value_type = number
	precision = 0

	category = country
}

fellowship_detection_factor = {
	color_type = good
	value_type = percentage
	precision = 3

	category = country
}