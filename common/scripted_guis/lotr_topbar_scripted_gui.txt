scripted_gui = {
	
	lotr_topbar_scripted_gui = {
		context_type = player_context
		parent_window_name = "lotr_topbar_guihook"

		window_name = "lotr_topbar_container"

		properties = {
		    
		}

		triggers = {
			ring_mechanic_view_button_click_enabled = { # TODO: needs tooltip
				has_global_flag = fs_started
				#NOT = { has_global_flag = fs_ring_seized }
				#NOT = { has_global_flag = fs_ring_destroyed } # TODO: Do we need these?
				# LTH needs to unlock this manually
				OR = {
					NOT = { tag = LTH }
					has_country_flag = lth_ring_mechanic_unlocked
				}
			}

			ring_mechanic_notification_glow_visible = {
				has_country_flag = ring_mechanic_notification_active
			}

			artifacts_notification_glow_visible = {
				has_country_flag = artifacts_notification_active
			}
        }

		effects = {
			ring_mechanic_view_button_click = {
				clr_country_flag = ring_mechanic_notification_active
				ring_toggle_details_view = yes
			}

			artifacts_view_button_click = {
				clr_country_flag = artifacts_notification_active
				lotr_artifacts_toggle_details_view = yes
			}
		}

		ai_enabled = {
			always = no
		}
		
		visible = {
			always = yes
		}
	}
}
